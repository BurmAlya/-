Мультисписок
Для реализации мультисписка (двусвязного списка) в C++ определяется структура Node, содержащая:
поле data для хранения целочисленного значения;
указатель prev на предыдущий узел;
указатель next на следующий узел.
Код определения структуры:
struct Node {
    int data;
    Node *prev;
    Node *next;
};
Очередь
В C++ очередь реализуется с помощью шаблона std::queue. Пример работы с очередью строк:
Создаётся объект queue типа std::queue<std::string>.
В очередь последовательно добавляются элементы: "я", "мы", "ты" с помощью метода push().
Код реализации:
std::queue<std::string> queue;
queue.push("я");
queue.push("мы");
queue.push("ты");
Дек (двусторонняя очередь)
Для работы с деком используется шаблон std::deque. Пример создания дека целых чисел:
объявляется объект d типа std::deque<int>;
инициализируется начальными значениями {9, 4, 6}.
Код реализации:
std::deque<int> d = {9, 4, 6};
Приоритетная очередь
Приоритетная очередь в C++ реализуется через шаблон std::priority_queue. Пример работы с целочисленной приоритетной очередью:
Создаётся объект pq типа std::priority_queue<int>.
В очередь добавляются элементы: 1, 27, 14 с помощью метода push().
Код реализации:
std::priority_queue<int> pq;
pq.push(1);     
pq.push(27);    
pq.push(14);
